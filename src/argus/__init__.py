"""Argus — camera-only physiological & behavioral sensing layer.

This package implements the deterministic, hardware-independent core specified in
``docs/`` (see NOTES.md for the scope split between this testable core and the
hardware adapters that run only on a self-hosted runner).
"""

__version__ = "0.1.0"
