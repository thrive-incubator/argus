# Argus — runtime requirements for the full live system.
# Pinned to the versions verified working in this project (Python 3.13, macOS arm64).
#
# SYSTEM DEP (not pip): the py-feat Action-Unit model needs the OpenMP runtime.
#   macOS:  brew install libomp     (see Brewfile)
#   Linux:  apt-get install libgomp1
#
# Install:  ./venv/bin/pip install -r requirements.txt

# --- core numerics ---
numpy==2.5.0
scipy==1.18.0

# --- camera + vision backbones (HR, HRV, respiration, blink, fidget, gaze-iris) ---
opencv-python==4.13.0.92
mediapipe==0.10.35

# --- signal processing / ground truth ---
neurokit2==0.2.13          # HRV (Kubios correction), respiration analysis
bleak==3.0.2               # Polar H10 over BLE (optional ground-truth reference)

# --- streaming / recording / bus ---
pylsl==1.18.2              # Lab Streaming Layer outlets (optional; needs liblsl native lib)
pyxdf==1.17.5              # XDF synchronized recording
python-osc==1.10.2         # OSC bridge for the art canvas (TouchDesigner)
websockets==16.0           # web dashboard backend

# --- perception models ---
onnxruntime==1.27.0
hsemotion-onnx==0.3.1      # affect: valence / arousal + emotion (fallback V/A head)
emotiefflib==1.1.1         # affect: newer multi-task V/A head (mobilevit_va_mtl); review §8
py-feat==2.0.2             # facial Action Units (pulls torch + xgboost; needs libomp)

# torch (~2.12) and xgboost (~3.3) arrive transitively via py-feat; pip resolves them.
